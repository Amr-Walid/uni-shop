# 🔧 تقرير تنفيذ المرحلة 0 + 1 — Backend جاهز للموبايل

> **التاريخ:** 2026-09-06
> **المرجع:** [`MOBILE_APP_PLAN_AR.md`](./MOBILE_APP_PLAN_AR.md)
> **الحالة:** ✅ **مكتملة ومُختبرة** — البناء 0 تحذير / 0 خطأ · الاختبارات 88/88 ناجحة

---

## 1. الملخص التنفيذي

تم تنفيذ **المرحلة 0 (التمهيد)** و**المرحلة 1 (توسيع الـ Backend)** بالكامل من الخطة. الـ API انتقل من **13 نقطة نهاية** إلى **52 نقطة نهاية موثقة** (57 عملية HTTP)، وأُغلقت **كل الفجوات الحاجبة** التي كانت تمنع بناء تطبيق موبايل.

### الأرقام

| المؤشر | قبل | بعد |
|--------|-----|-----|
| نقاط النهاية | 13 | **52** (57 عملية) |
| توثيق OpenAPI/Swagger | ❌ غير موجود | ✅ مولَّد من الكود |
| إصدارات الـ API | ❌ لا يوجد | ✅ `/api/v1` |
| مشروع اختبارات | ❌ لا يوجد | ✅ **88 اختبار ناجح** |
| حسابات العملاء | ❌ مستحيلة | ✅ كاملة |
| Pagination | ❌ لا يوجد | ✅ مع سقف 50 |
| Refresh Token | ❌ لا يوجد | ✅ مع تدوير وكشف سرقة |
| سياسات Rate Limiting | 1 | **7** |
| كيانات قاعدة البيانات | 17 | **22** |
| حجم عنصر القائمة | ~4KB | **~350 بايت** (−90%) |

### حالة التحقق

```
dotnet build FTD.Web.sln  →  Build succeeded. 0 Warning(s), 0 Error(s)   [6 مشاريع]
dotnet test FTD.Tests     →  Passed! Failed: 0, Passed: 88, Total: 88
API تشغيل حقيقي           →  52 path في swagger.json · ProblemDetails على 400/401/500
```

---

## 2. الفجوات المُغلقة (من الخطة)

| # | الفجوة | الحالة | التنفيذ |
|---|--------|--------|---------|
| **G-1** | لا حسابات عملاء | ✅ | `AppUser` + دور `Customer` + `SalesOrder.UserId` |
| **G-2** | DTOs ثقيلة | ✅ | `ProductListItemDto` (~350B بدل ~4KB) |
| **G-3** | لا Pagination | ✅ | `PagedResult<T>` بسقف 50 + ترتيب حتمي |
| **G-4** | لا Refresh Token | ✅ | تدوير + كشف إعادة استخدام + إبطال العائلة |
| **G-5** | لا سلة خادم/مفضلة | ✅ | `DbCartStorage` + `WishlistItem` |
| **G-6** | لا تتبع طلب/طلباتي | ✅ | `/orders/my` + `/orders/track` بعامل ثانٍ |
| **G-7** | لا Swagger | ✅ | Swashbuckle + XML docs |
| **G-8** | مسارات صور نسبية | ✅ | `MediaUrlHelper` + `Media:BaseUrl` |
| **G-9** | لا معالج أخطاء عام | ✅ | `GlobalExceptionHandler` + ProblemDetails |
| **G-10** | لا بحث/اقتراحات | ✅ | `/search` + `/search/suggest` |
| **G-11** | لا تسجيل أجهزة Push | ✅ | `DeviceToken` + `/devices/register` |
| **G-12** | لا محتوى/إعدادات في API | ✅ | `/content/*` + `/settings/public` + `/app/config` |

**الملاحظات الأمنية M-01 … M-10 من الخطة: أُغلقت كلها.**

---

## 3. ما تم بناؤه — طبقة بطبقة

### 3.1 `FTD.Domain` — 6 كيانات جديدة (إضافات فقط)

| الكيان | الغرض | قرار تصميمي مهم |
|-------|-------|-----------------|
| `AppUser : IdentityUser` | ملف العميل (اسم، عنوان، لغة) | أطوال العنوان **تطابق `SalesOrder`** حتى لا يفيض عمود عند النسخ للطلب |
| `RefreshToken` | جلسات طويلة قابلة للإبطال | يُخزَّن **الهاش SHA-256 فقط** — تسريب القاعدة لا يُمكن إعادة استخدامه |
| `WishlistItem` | المفضلة | فهرس فريد `(UserId, ProductId)` يجعل الإضافة idempotent |
| `UserCart` | سلة على الخادم | يحفظ **نفس صيغة JSON** لسلة الجلسة → إعادة استخدام `CartService` حرفياً |
| `DeviceToken` | أهداف الإشعارات | `UserId` قابل للإفراغ — **الضيوف يحتاجون إشعارات الطلب أيضاً** |
| `IdempotencyRecord` | منع تكرار الطلبات | يخزّن هاش الجسم لكشف إعادة استخدام المفتاح ببيانات مختلفة |

بالإضافة إلى `SalesOrder.UserId` (nullable — الطلبات كضيف كانت وستبقى مدعومة).

### 3.2 `FTD.Infrastructure` — الهجرة وخدمات الأمان

**Migration `AddMobileApiSupport`** — تم **تدقيقها يدوياً** بعد التوليد:

| التعديل اليدوي | السبب |
|----------------|-------|
| `AspNetUsers.CreatedAt` → `defaultValueSql: GETUTCDATE()` | التوليد التلقائي وضع `DateTime.MinValue`، فكان سيصبح تاريخ إنشاء كل حساب قائم **0001-01-01** |
| `IX_SalesOrders_OrderNumber` **غير فريد** | الفهرس الفريد كان سيُفشل الهجرة على أي قاعدة بها رقم طلب مكرر — والهجرات تعمل عند الإقلاع بسلوك fail-open، أي **الموقع يقع بشكل صعب التشخيص**. التفرّد يُضمن الآن وقت التوليد |

`Up()` **إضافي بالكامل** (تم التحقق: صفر `DropTable`/`DropColumn`/`DeleteData`).

**`JwtTokenService`** — أُصلحت ثلاث نقاط:
- `Encoding.ASCII` → **`UTF8`**: ASCII يستبدل كل بايت غير-ASCII بـ `?` صامتاً، فيقلّص فضاء المفتاح الفعلي.
- يرفض أي سر **أقل من 256 بت**.
- توكن وصول **15 دقيقة** بدل 120، وrefresh عشوائي من CSPRNG يُعاد مرة واحدة فقط.

**`AuthService`** — النقطة الأمنية الأدق في العمل كله:
- **تدوير الـ Refresh Token**: كل تجديد يُبطل المُقدَّم ويصدر خليفة.
- **كشف إعادة الاستخدام**: تقديم توكن مُبطَل يعني أن العميل الشرعي دوّره بالفعل → النسخة هذه **مسروقة**. لا يمكن التمييز بين المهاجم والضحية، لذا **تُبطَل العائلة كاملة**. بدون هذا، توكن مسروق = وصول دائم.
- كل تسجيل دخول يبدأ **عائلة جديدة**، حتى لا يؤدي إبطال سلسلة مخترقة لطرد المستخدم من أجهزته الأخرى.
- تفعيل **الـ Lockout**: الكود القديم كان يمرر `lockoutOnFailure: false`، أي أن **الـ API كان يتجاوز حماية القوة الغاشمة** التي يفرضها الموقع.
- `ForgotPassword` **يرجّع نجاحاً دائماً** حتى للبريد غير الموجود — وإلا صار الـ endpoint أداة لحصر الحسابات المسجلة.
- `DeleteAccount` **يُخفي الهوية ولا يحذف الصف**: الحذف كان سيُدمّر تاريخ الطلبات (تقارير الإيرادات) أو يتركها معلّقة.

> **ملاحظة معمارية:** `AuthService` يستخدم `UserManager` فقط ولا يستخدم `SignInManager`، لأن الأخير يعيش في تجميعة ASP.NET Core وكان سيجرّ اعتمادية استضافة ويب إلى طبقة البنية التحتية. `UserManager` يوفّر كل ما يحتاجه تدفق التوكن.

### 3.3 `FTD.Application` — العقود والاستعلامات

**`CatalogQueryService`** — منفصل عن `IProductService` بشكل مقصود: الأخير يخدم الموقع ولوحة الأدمن ويرجّع `ProductDto` الغني، وإضافة تحميلات مفرطة للـ paging كانت ستضخّم واجهة بها 30+ عضواً وتخاطر بتغيير سلوك يعتمد عليه الموقع.

**أهم سطر في الخدمة كلها:**

```csharp
_ => query.OrderByDescending(p => p.IsFeatured).ThenBy(p => p.SortOrder).ThenBy(p => p.Id)
//                                                                       ^^^^^^^^^^^^^^^^^
```

`ThenBy(p => p.Id)` ليس تجميلاً. مع منتجات كثيرة تتشارك `SortOrder = 0`، الترتيب غير الحتمي يجعل SQL Server يرجّع الصفوف بترتيب مختلف بين استعلام واستعلام → **نفس المنتج يظهر في صفحتين ومنتج آخر لا يظهر أبداً**. في Infinite Scroll يعني تكرارات وعناصر مفقودة صامتة. (نفس صنف الخطأ I-25 في `AUDIT_REPORT.md`.)

**قرارات أخرى:**
- فلترة المواصفات = `EXISTS` لكل قيمة **في SQL** (الموقع يفلتر في الذاكرة، وهو ما لا يعمل مع paging لأن `TotalCount` يجب أن يُحسب في القاعدة).
- عدّ التصنيفات/الماركات بإسقاط `COUNT` مترابط (يتجنب I-08).
- `/home` المُجمَّع ينتظر الاستعلامات **بالتسلسل لا `Task.WhenAll`** — لأن `DbContext` ليس آمناً للخيوط وكلها تتشارك النسخة نفسها؛ التوازي كان سيرمي *"A second operation was started on this context"*.

**`DbCartStorage`** — أجمل مكسب معماري: لأن `CartService` يطلب "مخزناً" مجرداً فقط، صارت السلة على الخادم **بدون تغيير حرف واحد** في منطق السلة. الموقع يبقى على `SessionCartStorage`.

### 3.4 `FTD.Api` — إعادة بناء كاملة

| المكوّن | التنفيذ |
|--------|---------|
| **Swagger/OpenAPI** | مولَّد من تعليقات XML → العقد **مستمد من الكود ولا يمكن أن يكذب** |
| **الإصدارات** | `/api/v1` عبر مقطع URL — نسخ المتاجر المنشورة تستمر على إصدارها |
| **الأخطاء** | `ProblemDetails` (RFC 7807) + `errorCode` ثابت في كل فشل |
| **Rate Limiting** | من سياسة واحدة → **7 سياسات** + سقف قراءة 300/دقيقة، مقسّمة بالـ IP، مع `Retry-After` |
| **JWT Events** | تمييز `TOKEN_EXPIRED` من `UNAUTHORIZED` ليعرف العميل متى يجدّد صامتاً ومتى يطرد المستخدم |
| **السر** | أُزيل من `appsettings.json`؛ **الإقلاع يفشل فوراً** إن كان مفقوداً أو ما زال النص المُودَع |

**7 متحكمات:** Auth · Catalog · Cart · Orders · Me · Content · Admin
**+ `LegacyCompatibilityController`:** المسارات الخمسة الأصلية تعمل بأشكالها **الأصلية** مع ترويسات `Deprecation`/`Link`/`Warning` — تغيير استجابة مسار قائم هو بالضبط الكسر الذي وُجدت الإصدارات لتجنّبه.

---

## 4. القرارات الأمنية والمبرر

| القرار | المبرر |
|-------|--------|
| `Stock` الرقمي **لا يُرسل** للعميل — فقط `InStock`/`LowStock` | معلومة تجارية. اختبار يتحقق بالـ reflection أن الحقل غير موجود في الـ DTO |
| `/orders/track` يشترط **آخر 4 أرقام هاتف** | أرقام الطلبات نمطها متوقع (`FTD`+طابع زمني+3 أرقام)، فالرقم وحده **ليس سراً**. بدون عامل ثانٍ = تسريب أسماء وهواتف وعناوين العملاء |
| ملكية الطلب مفروضة **داخل الاستعلام** | يمنع إدخال IDOR بنسيان فحص في نقطة نداء جديدة |
| طلب مستخدم آخر يرجّع **404 لا 403** | الـ 403 يؤكّد أن الطلب موجود — تسريب معلومة بنفسه |
| Checkout **يعيد قراءة كل الأسعار** من القاعدة | العميل قابل للهندسة العكسية؛ الخادم هو الحكم الوحيد على السعر والتوفر |
| `Idempotency-Key` على checkout | شبكات الموبايل تُسقط الاستجابات، والعميل لا يميّز "لم يُنشأ" من "أُنشئ وفُقد الرد" فيعيد المحاولة → طلب مكرر وخصم مخزون مرتين |
| `DELETE /auth/account` | **شرط إلزامي** من App Store (5.1.1(v)) — غيابه رفض مؤكد |
| إلغاء الطلب **يعيد المخزون** في نفس `SaveChanges` | معاملة واحدة ضمنية: لا يمكن تطبيق تغيير الحالة بدون إعادة الكمية |

---

## 5. الاختبارات — 88 اختبار

المشروع كان **بلا أي مشروع اختبارات**. هذا كان مقبولاً بينما التغييرات إضافية، لكن هذا العمل **يعيد كتابة نوع Identity** الذي يستخدمه الموقع ولوحة الأدمن الحاليان — بلا شبكة أمان.

| المجموعة | العدد | أهم ما تحميه |
|---------|-------|-------------|
| `CatalogQueryServiceTests` | 16 | **حارس انحدار الـ paging**: كل منتج بـ `SortOrder = 0` (تعادل تام)، ويتحقق أن الصفحتين معاً تحويان الـ 10 معرّفات كلها بلا تكرار — يفشل فوراً لو أُزيل `ThenBy(Id)` |
| `OrderServiceTests` | 16 | خصم/إعادة المخزون · IDOR · العامل الثاني للتتبع · تفرّد أرقام الطلبات على 15 طلباً متتالياً |
| `HelperAndServiceTests` | 56 | التوطين · URLs · الخصم · `FeaturesJson` التالف · idempotency · `DbCartStorage` · **`CartService` عبر المخزن الجديد** |

**أُصلح أثناء الكتابة:** حالات "لا خصم" تستخدم `MemberData` لأن سمات C# لا تحمل ثوابت `decimal`، فكان xUnit يمرّر `int` ويفشل في تحويله إلى `decimal?`.

---

## 6. مطلوب قبل النشر للإنتاج ⚠️

هذه **ليست اختيارية**:

```bash
# 1) توليد سر JWT قوي (الإقلاع يفشل بدونه)
export JwtSettings__Secret="$(openssl rand -base64 48)"

# 2) نطاق الوسائط — الصور تعيش في wwwroot الخاص بـ FTD.Web
export Media__BaseUrl="https://uni-shop.com"

# 3) أصول المتصفح المسموحة (تطبيقات الموبايل الأصلية لا ترسل Origin ولا تتأثر)
export Cors__AllowedOrigins__0="https://uni-shop.com"
```

| # | المهمة | الأهمية |
|---|-------|---------|
| 1 | 🚨 **تدوير سر JWT القديم** — القيمة القديمة موجودة في تاريخ Git | حرجة |
| 2 | تطبيق الهجرة على **نسخة من الإنتاج أولاً** | حرجة |
| 3 | **اختبار يدوي كامل للوحة الأدمن** بعد تغيير Identity | حرجة |
| 4 | ضبط `Media:BaseUrl` وإلا لن تظهر الصور في التطبيق | عالية |
| 5 | ضبط SMTP لتفعيل استعادة كلمة المرور | عالية |

---

## 7. الخطوة التالية

الـ Backend **جاهز**. عقد الـ API مستقر ومنشور على `/swagger` — وهو ما سيولّد فريق Flutter نماذج Dart منه.

المسار من الخطة:
1. **المرحلة 2** — تطبيق Flutter (الأساس + الكتالوج + السلة + الطلب) → MVP قابل للنشر
2. **المرحلة 3** — شاشات الحسابات (الـ API لها جاهز بالكامل الآن)
3. **المرحلة 4** — الإشعارات (`DeviceToken` والـ endpoint جاهزان؛ يتبقى خطّاف FCM في `UpdateStatusAsync`)

> **تذكير من الخطة:** أهم توصية كانت "لا تبدأ كود Flutter قبل إنهاء المرحلة 1". **المرحلة 1 انتهت الآن** — يمكن البدء بأمان.
